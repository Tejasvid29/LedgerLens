// Superseded by app/page.tsx (Server Component) + Sidebar/HoldingsSummary/
// TransactionTable/SyncControls. This file's sandbox couldn't delete files
// on the mounted folder, so this is a stub rather than a removal — safe to
// `rm apps/web/src/components/Dashboard.tsx` locally, nothing imports it.
export {};
